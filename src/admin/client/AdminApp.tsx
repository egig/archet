/** Placeholder root component — proves the build -> manifest -> serve -> browser
 * pipeline end to end. The real admin UI (model list/detail views, auth, etc.)
 * is future work. */
export function AdminApp() {
  return (
    <div>
      <h1>archet admin</h1>
      <p>Admin UI not implemented yet.</p>
    </div>
  );
}
