/** Injected by esbuild's `define` at build time (see src/cli/build-console.ts) from
 * `FrameworkConfig.consolePath` — the path this console client is mounted at, e.g. '/console'. */
declare const __CONSOLE_PATH__: string;
